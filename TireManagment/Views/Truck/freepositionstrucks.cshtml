@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@using TireManagment.DbModels;
@inject TireManagment.Services.TruckService truckservice;
    
    @foreach (var group in ViewBag.freepositionstrucks)
    {
       Truck truck=truckservice.GeTruck(group.Key);
        <span>TruckNumber : @group.Key</span>
      
        <table class="table table-dark">
            @*<tr>

                <th>@truck.TruckNumber</th>
                <th>@truck.VehichleModelNo</th>
                <th>@truck.AxleCount</th>
                <td><a class="btn btn-success">Assign Tires</a></td>
            </tr>*@
            <tr>
                <td>Free Positions</td>
                @foreach (var x in group)
                {

                    <td> @x.TirePosition</td>

                }
                <td><a class="btn btn-success" asp-action="assigntires" asp-controller="Truck" asp-route-trucknumber="@truck.TruckNumber">Assign Tires</a></td>
            </tr>
            </table>
    }

